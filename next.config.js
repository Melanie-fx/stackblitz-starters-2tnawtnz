/** @type {import('next').NextConfig} */
const nextConfig = {
  // You can leave this empty or add other config options here
  reactStrictMode: true,
  swcMinify: true,
  // If you're using server components or special features, add them here
};

module.exports = nextConfig;
